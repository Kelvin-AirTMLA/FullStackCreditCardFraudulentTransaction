name: Keep ML API Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping ML API health
        run: curl -fsS --max-time 10 https://fullstackcreditcardfraudulenttransaction.onrender.com/health