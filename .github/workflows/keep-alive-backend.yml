name: Keep Backend Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Node backend health
        run: curl -fsS --max-time 10 https://fullstackcreditcardfraudulenttransaction-p74j.onrender.com/health